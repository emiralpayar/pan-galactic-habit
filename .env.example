# Copy to .env for local development. Never commit .env.
# Deployed environments read these from a secret manager.

# ── Azure DevOps (adapters/azure-devops) ─────────────────────────────────
# Organization URL, e.g. https://dev.azure.com/<organization>
AZURE_DEVOPS_ORG_URL=
AZURE_DEVOPS_PROJECT=
# Least-privilege token: Work Items (Read & write) and Wiki (Read) scopes only.
# The identity's project permissions must still deny state changes and deletion.
AZURE_DEVOPS_PAT=

# ── Agent Runtime (docs/adr/0004-agent-runtime-port-with-claude-and-copilot-backends.md) ──
# Credentials only: each lobe's backend and model are committed configuration, not variables.
# Tokens are personal: use your own subscriptions, never someone else's.
# Claude backend: your own subscription token from `claude setup-token` (single-user runs only).
CLAUDE_CODE_OAUTH_TOKEN=
# Copilot backend: your own fine-grained token with only the Copilot Requests permission and
# no repository access (not the token that pushes branches or opens PRs).
COPILOT_GITHUB_TOKEN=
# Instead of CLAUDE_CODE_OAUTH_TOKEN (set only one): pay-as-you-go Anthropic API key.
# ANTHROPIC_API_KEY=
